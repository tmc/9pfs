# Binary left by `go run .` (named after the module directory).
/9pfs

# c-archive build outputs (the generated header sits next to the archive).
*.a
libNinePFS.h
*.dylib

# Swift build caches.
.build/
